# 微信云托管 · 服务环境变量清单（复制到「服务设置 → 环境变量」）
# 数据库信息在：云托管控制台 → MySQL

# ---- 必填：数据库（推荐用云托管官方变量名）----
MYSQL_ADDRESS=10.36.107.20:3306
MYSQL_USERNAME=Cby_xw
MYSQL_PASSWORD=在此填写MySQL密码
MYSQL_DATABASE=wanyehulian

# ---- 必填：业务 ----
JWT_SECRET=请改成随机长字符串
ADMIN_CODE=A001
PORT=80
NODE_ENV=production

# ---- 对象存储（运营台图片/文件上传）----
COS_BUCKET=7072-prod-d3g1vkrj3290d085e-1467541248
COS_REGION=ap-shanghai
COS_MODE=cloud
WX_CLOUD_ENV=prod-d3g1vkrj3290d085e
# 云托管容器内一般通过开放接口自动取临时密钥，无需 COS_SECRET_ID
# 若本地调试直连 COS，可另配：
# COS_SECRET_ID=
# COS_SECRET_KEY=

# ---- 可选：小程序 ----
# WX_APPID=
# WX_APP_SECRET=

# 说明：
# 1. 不要把 DB_HOST 配成 127.0.0.1（那是本机，云托管容器里没有 MySQL）
# 2. 改完环境变量后必须「发布新版本」才会生效
# 3. 发布后先打开 /api/health，应看到 db.ok=true 且 host 为内网 IP
# 4. 上传依赖云托管「对象存储」；服务需与存储同环境，才能用临时密钥上传
